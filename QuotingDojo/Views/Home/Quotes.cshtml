<h1>Here are the awesome quotes!</h1>
@foreach(var user in @ViewBag.Query)
{
    <div class="border-bottom-dark">
        <p>"@user["quote"]"</p>
        <p class="ml-5">-@user["name"] at @user["created_at"].ToString("h:mmtt MMMM dd yyyy")</p>
    </div>
}
